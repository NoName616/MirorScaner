# Основные зависимости для проекта MirrorScan
numpy>=1.20
matplotlib>=3.5
pydantic>=1.10   # используется в scan/stepper_config.py
pyserial>=3.5     # для связи с контроллером STM32
opencv-python>=4.5  # для гомографии (опционально, но рекомендуется)
scipy>=1.7        # для функций distance (опционально)
PyQt5>=5.15       # для графического интерфейса (ui/*.py)
